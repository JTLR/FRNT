block variables

doctype html
html(lang="en")
	include ../partials/_head

	body
		.Wrapper

			include ../partials/_header

			block content

			include ../partials/_footer
	
	if debug
		script(src="//localhost:35729/livereload.js")
		<script>document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] + ':8080/js/app.js" async defer></' + 'script>')</script>
	else
		script(src="./js/app.js")